<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Daftar Produk</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .produk {
            border: 1px solid #ccc;
            padding: 20px;
            margin-bottom: 20px;
            width: 300px;
            text-align: center;
        }
        .produk img {
            width: 100%;
            height: auto;
        }
    </style>
</head>
<body>
    <h1>Orang di The Office April 2025</h1>

    <% if (products.length > 0) { %>
        <% products.forEach(product => { %>
            <div class="produk">
                <h2><%= product.nama %></h2>
                <img src="<%= product.link_gambar %>" alt="<%= product.nama %>">
                <p><strong>Harga:</strong> Rp <%= Number(product.harga).toLocaleString('id-ID') %></p>
            </div>
        <% }) %>
    <% } else { %>
        <p>Tidak ada produk tersedia.</p>
    <% } %>

</body>
</html>
